[pytest]
# A camada Python vive em python/. Com pythonpath aqui, `pytest` funciona
# rodado da raiz do repo OU de dentro de python/ — em ambos o rootdir é a
# raiz e os módulos de python/ entram no sys.path.
pythonpath = python
testpaths = python/tests
